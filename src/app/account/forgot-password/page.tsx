"use client";
import Link from "next/link";
import { useState } from "react";
import { requestPasswordReset } from "@/lib/auth-client";
export default function ForgotPasswordPage() { const [email, setEmail] = useState(""); const [message, setMessage] = useState(""); const [pending, setPending] = useState(false); async function submit(event: React.FormEvent) { event.preventDefault(); setPending(true); const result = await requestPasswordReset({ email, redirectTo: `${window.location.origin}/account/reset-password` }); setPending(false); setMessage(result.error ? result.error.message || "We could not send that reset email." : "If an account exists for this email, a reset link is on its way."); } return <main className="mx-auto min-h-screen max-w-xl px-5 py-20"><Link href="/account/sign-in" className="text-xs font-semibold uppercase tracking-[.14em] text-[#d3146d] underline">Back to sign in</Link><p className="mt-12 text-xs font-semibold uppercase tracking-[.16em] text-[#d3146d]">Account recovery</p><h1 className="mt-4 text-5xl font-medium tracking-[-.06em]">Reset your password.</h1><form onSubmit={submit} className="mt-9 space-y-5"><label className="block text-sm">Email address<input type="email" required value={email} onChange={(event) => setEmail(event.target.value)} className="mt-2 w-full border border-black px-4 py-3" /></label>{message && <p className="border-l-4 border-[#d3146d] bg-[#f8dbe9] px-4 py-3 text-sm">{message}</p>}<button disabled={pending} className="bg-black px-6 py-4 text-xs font-semibold uppercase tracking-[.14em] text-white">{pending ? "Sending..." : "Send reset link"}</button></form></main>; }
